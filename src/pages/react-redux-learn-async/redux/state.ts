import { pageState } from '../interface/types';
export const initialState: pageState = {
  dataList: [{
    age: 12,
    name: 'wasdfga',
    sex: 'man',
  }],
  flag: true,
  loading: true,
  fetchData: {},
}